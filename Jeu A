#A est un simple jeu de pile ou face avec une pièce biaisée (pile avec une probabilité de p=0.49). 
#Le joueur mise un euro et lance la pièce : 
# s’il obtient pile il gagne un euro en plus de récupérer sa mise
# sinon il perd sa mise.

import random
capital = 1000
nombredelancers = 500


def lancer_de_pieceA():
    resultat = random.choices(['Face', 'Pile'], weights=[51, 49], k=1)
    return resultat[0]

for i in range (nombredelancers):
    resultat = lancer_de_pieceA()
    if resultat == 'Pile':
        capital += 1
    else:
        capital -= 1
    print("Lancer de pièce A :", resultat)
    print("Après ce lancé, mon capital est de",capital,"euros")
print("Après",nombredelancers,"lancers, mon capital final est de",capital,"euros")